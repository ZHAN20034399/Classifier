# SulMineralClassifier - 依赖库 / Dependencies
# 安装方式 / Install: pip install -r requirements.txt

numpy>=1.21.0
pandas>=1.3.0
openpyxl>=3.0.9          # Excel 文件读写 / Excel read/write
scikit-learn>=1.0.0
xgboost>=1.5.0
matplotlib>=3.4.0
seaborn>=0.11.0
shap>=0.41.0
imbalanced-learn>=0.8.0  # imblearn (SMOTE 等过采样方法) / oversampling methods
Pillow>=8.0.0             # 图像处理 / Image processing
